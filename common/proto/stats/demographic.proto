syntax = "proto3";

package dat.project;

option go_package = "project.dat.works";
option java_package = "works.dat.project";

message Demographic {
    // Household types (single, married couple, single parent, etc)
    // Age Distribution
    // Gender ratio
    // Population growth
}